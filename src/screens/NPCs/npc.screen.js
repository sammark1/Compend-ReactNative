import React from "react";
import { Text } from "react-native";

import { SafeView } from "../../infrastructure/util/safe-area.component";

export const NPCs = () => {
  return (
    <SafeView>
      <Text>Hello Butt</Text>
    </SafeView>
  );
};
