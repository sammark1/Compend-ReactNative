import React from "react";
import { Text } from "react-native";

import { SafeView } from "../../infrastructure/util/safe-area.component";

export const NPCCreate = () => {
  return (
    <SafeView>
      <Text>NPC Create</Text>
    </SafeView>
  );
};
